models:
  demo_linear_Alpha158:
    algorithm: linear
    dataset: Alpha158
    market: csi300
    yaml_file: config/workflow_config_demo_weekly.yaml
    enabled: true
    tags: [baseline]

  # --- 预训练示例 (Pre-training Examples) ---
  # demo_lstm:
  #   algorithm: lstm
  #   dataset: Alpha158
  #   market: csi300
  #   yaml_file: config/workflow_config_lstm.yaml  # 需要对应的 YAML
  #   enabled: false
  #   tags: [basemodel, ts]
  #
  # demo_gats:
  #   algorithm: gats
  #   dataset: Alpha158
  #   market: csi300
  #   yaml_file: config/workflow_config_gats.yaml  # 需要对应的 YAML
  #   enabled: false
  #   tags: [graph, ts]
  #   pretrain_source: demo_lstm

  # --- 随机猴子基准 (Random Monkey Benchmark) ---
  random_monkey_Alpha158:
    algorithm: random
    dataset: Alpha158
    market: csi300
    yaml_file: config/workflow_config_random.yaml
    enabled: false
    tags: [benchmark]
    notes: "随机排序猴子基准 — 模型跑不赢猴子说明没有 alpha"

